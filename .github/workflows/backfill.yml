name: Manual Backfill (Jan-Feb)

on:
  workflow_dispatch: # 수동 실행 버튼

jobs:
  run-backfill:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Seoul
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      NAVER_ADS_API_KEY: ${{ secrets.NAVER_ADS_API_KEY }}
      NAVER_ADS_SECRET: ${{ secrets.NAVER_ADS_SECRET }}
      CUSTOMER_ID: "1346816" # 본인 ID 확인

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests python-dotenv sqlalchemy psycopg2-binary

      - name: Run Backfill Loop (Python)
        shell: python
        run: |
          import subprocess
          import time
          from datetime import date, timedelta

          # 시작일과 종료일 설정 (여기만 고치면 됩니다)
          start_date = date(2026, 1, 1)
          end_date = date(2026, 2, 14)

          current = start_date
          while current <= end_date:
              d_str = current.strftime("%Y-%m-%d")
              print(f"🚀 [수집 시작] 날짜: {d_str}")
              
              # collector.py 실행
              subprocess.run(["python", "collector.py", "--date", d_str])
              
              # 하루 증가
              current += timedelta(days=1)
              time.sleep(1) # 서버 보호를 위해 1초 휴식
