name: Patch Missing Dates (Hole Fixer)

on:
  workflow_dispatch:

jobs:
  patch-holes:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1 # 안전하게 하루치씩 순서대로
      fail-fast: false
      matrix:
        target_date:
          # 대표님이 찾아내신 빵꾸난 날짜 6개만 족집게처럼 수집!
          - "2026-01-09"
          - "2026-01-18"
          - "2026-01-21"
          - "2026-01-24"
          - "2026-01-27"
          - "2026-01-30"

    env:
      TZ: Asia/Seoul
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      NAVER_ADS_API_KEY: ${{ secrets.NAVER_ADS_API_KEY }}
      NAVER_ADS_SECRET: ${{ secrets.NAVER_ADS_SECRET }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests python-dotenv sqlalchemy psycopg2-binary pandas openpyxl

      - name: Run Hole Fixer
        run: |
          echo "🚀 빵꾸난 날짜 수집 시작: ${{ matrix.target_date }}"
          python collector.py --date ${{ matrix.target_date }} --skip_dim --workers 10
